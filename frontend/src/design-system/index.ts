/**
 * FISCALFLOW DESIGN SYSTEM
 *
 * Premium UI Framework para aplicação SaaS
 *
 * @example
 * import { Button, Input, Card, colors, spacing } from '@/design-system'
 * import { ThemeProvider } from '@/design-system'
 */

// Tokens
export * from './tokens';

// Componentes
export * from './components';

// Tema
export { ThemeProvider, useTheme, useThemeToggle, type Theme } from './theme/ThemeProvider';
