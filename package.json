{
  "name": "fiscalflow",
  "version": "0.1.0",
  "private": true,
  "description": "SaaS multi-tenant de automação fiscal para escritórios de contabilidade",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.3.0"
  },
  "lint-staged": {
    "backend/**/*.ts": "npx --prefix backend eslint --fix",
    "frontend/**/*.{ts,tsx}": "npx --prefix frontend eslint --fix"
  }
}
